package advancedSearchTree.redBlack;

import binarySearchTree.BSTImpl;

/**
 * 支持对历史版本的访问 ：每次动态操作所产生的结构变化量不致超过O(1)，所以需要红黑树
 *
 * @param <T>
 */
public class RedBlack<T extends Comparable> extends BSTImpl<T> {
}
